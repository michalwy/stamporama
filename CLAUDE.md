Read AGENTS.md before starting any task — with `git show origin/main:AGENTS.md` until you have cut your branch, because a worktree is only as fresh as the last time it was cut. It contains the project rules, the invariants that hold on every task, and a topic map pointing at `docs/agents/*.md` — read the topic file for the area you are about to change. AGENTS.md's *starts from a fresh `main`* bullet is the record of what runs before what, and this line summarises it: where the two disagree, that bullet is right.
